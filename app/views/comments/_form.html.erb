<%= form_for([@article, @article.comments.build], remote: true) do |f| %> 
<p>
<%= current_user %><br>
</p>
 <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
